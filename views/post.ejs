<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= post.title %> - Your Website Name</title>
    <meta name="description" content="<%= post.description %>">
    <meta name="keywords" content="<%= post.tags %>">
    <link rel="stylesheet" href="/css/styles.css"> <!-- Adjusted the path to match your index page -->
    <style>
        /* Base styles */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            background-color: #ffffff;
            color: #333333;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        article {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            background-color: #f9f9f9;
            border-radius: 8px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        h1, h2 {
            margin: 0 0 20px 0;
            text-align: center;
        }

        h1 {
            font-size: 32px;
            color: #007BFF;
        }

        h2 {
            font-size: 24px;
            color: #555555;
        }

        .description {
            font-size: 16px;
            line-height: 1.6;
            color: #666666;
            margin-bottom: 20px;
        }

        img, video {
            max-width: 100%;
            height: auto;
            display: block;
            margin: 20px auto;
            border-radius: 4px;
        }

        a {
            display: block;
            margin: 20px 0;
            text-align: center;
            text-decoration: none;
            color: #007BFF;
            font-weight: bold;
        }

        a:hover {
            text-decoration: underline;
        }

        /* Dark mode styles */
        @media (prefers-color-scheme: dark) {
            body {
                background-color: #121212;
                color: #ffffff;
            }

            article {
                background-color: #1e1e1e;
            }

            h1 {
                color: #bb86fc;
            }

            h2, .description {
                color: #e0e0e0;
            }

            a {
                color: #bb86fc;
            }
        }
    </style>
</head>

<body>
    <article>
        <h1><%= post.title %></h1>
        <h2><%= post.subtitle %></h2>
        <div class="description"><%= post.description %></div>

        <% if (post.imageUrl) { %>
            <img src="<%= post.imageUrl %>" alt="<%= post.title %>">
        <% } %>

        <% if (post.videoUrl) { %>
            <video controls>
                <source src="<%= post.videoUrl %>" type="video/mp4">
                Your browser does not support the video tag.
            </video>
        <% } %>
    </article>

    <a href="/">Back to Posts</a>
</body>

</html>
